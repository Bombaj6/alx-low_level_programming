#include <stdio.h>
/**
 * main - Numbers prints five
 *
 * Return:Always 0 (Sucess)
 *
 */
int main(void)
{
	char n;

	for (n = '0' ; n <= '9'; n++)
	{
		putchar(n);
	}

	putchar('\n');

		return (0);
}
